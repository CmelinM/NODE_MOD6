/**
 * Controlador de productos
 */

export const productsController = async (req, res, urlParts, payload) => {

  res.writeHead(501, 'Not Implemented', { "content-type": "text/plain" })
  res.write('Proximamente productos')
}